import type { ReactNode } from "react";

/** Feature-layer shell for vms. Compose feature pages/components here. */
export function VmsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
