import type { ReactNode } from "react";

/** Feature-layer shell for performance. Compose feature pages/components here. */
export function PerformanceFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
