import type { ReactNode } from "react";

/** Feature-layer shell for file-history. Compose feature pages/components here. */
export function FileHistoryFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
