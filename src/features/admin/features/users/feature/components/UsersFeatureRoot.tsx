import type { ReactNode } from "react";

/** Feature-layer shell for users. Compose feature pages/components here. */
export function UsersFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
