import type { ReactNode } from "react";

/** Feature-layer shell for onboarding. Compose feature pages/components here. */
export function OnboardingFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
