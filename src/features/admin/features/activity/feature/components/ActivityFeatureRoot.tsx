import type { ReactNode } from "react";

/** Feature-layer shell for activity. Compose feature pages/components here. */
export function ActivityFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
