import type { ReactNode } from "react";

/** Feature-layer shell for processing-logs. Compose feature pages/components here. */
export function ProcessingLogsFeatureRoot({
	children,
}: {
	children?: ReactNode;
}) {
	return <>{children}</>;
}
