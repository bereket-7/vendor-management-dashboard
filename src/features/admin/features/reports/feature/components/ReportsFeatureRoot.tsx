import type { ReactNode } from "react";

/** Feature-layer shell for reports. Compose feature pages/components here. */
export function ReportsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
