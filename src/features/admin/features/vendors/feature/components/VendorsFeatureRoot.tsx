import type { ReactNode } from "react";

/** Feature-layer shell for vendors. Compose feature pages/components here. */
export function VendorsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
