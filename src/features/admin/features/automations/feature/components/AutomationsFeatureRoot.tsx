import type { ReactNode } from "react";

/** Feature-layer shell for automations. Compose feature pages/components here. */
export function AutomationsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
