import type { ReactNode } from "react";

/** Feature-layer shell for settings. Compose feature pages/components here. */
export function SettingsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
