import type { ReactNode } from "react";

/** Feature-layer shell for contracts. Compose feature pages/components here. */
export function ContractsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
