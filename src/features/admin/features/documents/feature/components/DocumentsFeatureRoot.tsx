import type { ReactNode } from "react";

/** Feature-layer shell for documents. Compose feature pages/components here. */
export function DocumentsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
