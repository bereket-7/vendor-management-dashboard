import type { ReactNode } from "react";

/** Feature-layer shell for categories. Compose feature pages/components here. */
export function CategoriesFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
