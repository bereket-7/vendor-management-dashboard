import type { ReactNode } from "react";

/** Feature-layer shell for approvals. Compose feature pages/components here. */
export function ApprovalsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
