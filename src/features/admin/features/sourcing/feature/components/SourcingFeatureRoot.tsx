import type { ReactNode } from "react";

/** Feature-layer shell for sourcing. Compose feature pages/components here. */
export function SourcingFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
