import type { ReactNode } from "react";

/** Feature-layer shell for vendor-comparison. Compose feature pages/components here. */
export function VendorComparisonFeatureRoot({
	children,
}: {
	children?: ReactNode;
}) {
	return <>{children}</>;
}
