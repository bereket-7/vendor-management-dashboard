import type { ReactNode } from "react";

/** Feature-layer shell for providers. Compose feature pages/components here. */
export function ProvidersFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
