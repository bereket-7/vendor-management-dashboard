import type { ReactNode } from "react";

/** Feature-layer shell for roles. Compose feature pages/components here. */
export function RolesFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
