import type { ReactNode } from "react";

/** Feature-layer shell for schedules. Compose feature pages/components here. */
export function SchedulesFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
