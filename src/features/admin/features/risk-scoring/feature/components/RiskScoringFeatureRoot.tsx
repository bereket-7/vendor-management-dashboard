import type { ReactNode } from "react";

/** Feature-layer shell for risk-scoring. Compose feature pages/components here. */
export function RiskScoringFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
