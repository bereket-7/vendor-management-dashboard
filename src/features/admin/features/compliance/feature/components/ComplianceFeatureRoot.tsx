import type { ReactNode } from "react";

/** Feature-layer shell for compliance. Compose feature pages/components here. */
export function ComplianceFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
