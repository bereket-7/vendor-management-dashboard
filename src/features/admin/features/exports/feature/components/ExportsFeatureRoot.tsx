import type { ReactNode } from "react";

/** Feature-layer shell for exports. Compose feature pages/components here. */
export function ExportsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
