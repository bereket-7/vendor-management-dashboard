import type { ReactNode } from "react";

/** Feature-layer shell for members. Compose feature pages/components here. */
export function MembersFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
