import type { ReactNode } from "react";

/** Feature-layer shell for invoices. Compose feature pages/components here. */
export function InvoicesFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
