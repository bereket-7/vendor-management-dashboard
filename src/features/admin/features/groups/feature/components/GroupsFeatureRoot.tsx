import type { ReactNode } from "react";

/** Feature-layer shell for groups. Compose feature pages/components here. */
export function GroupsFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
